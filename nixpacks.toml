[phases.setup]
nixPkgs = ["...", "ffmpeg", "yt-dlp", "python3", "deno"]

[phases.install]
cmds = ["npm ci", "python3 -m venv /opt/ytdlp-venv && /opt/ytdlp-venv/bin/pip install yt-dlp bgutil-ytdlp-pot-provider && echo '✅ PO token plugin installed successfully' || echo '⚠️ PO token plugin install failed - will use system yt-dlp'"]
nixPkgs = ["...", "ffmpeg", "python3", "deno", "chromium"]
